{
  "name": "image-fallback",
  "version": "1.0.0",
  "description": "Image Fallback is a Google Chrome extension that allows you to specify a fallback host to retrieve images from if they cannot be found on the original host. This helps developers keep their image assets in sync between their local, staging and production environments.",
  "dependencies": {},
  "devDependencies": {
    "grunt": "~0.4.4",
    "grunt-contrib-clean": "~0.5.0",
    "grunt-mkdir": "~0.1.1",
    "grunt-contrib-copy": "~0.5.0",
    "grunt-contrib-jshint": "~0.10.0",
    "browserify": "~3.39.0",
    "grunt-browserify": "~2.0.3",
    "grunt-contrib-uglify": "~0.4.0"
  },
  "engines": {
    "node": ">= 0.10.0"
  },
  "export-symbol": "chromeImageFallback.exports"
}
